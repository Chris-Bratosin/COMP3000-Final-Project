# Future Schema Archive

These Mongoose models were created for an earlier multi-user and workspace roadmap.
They are not imported by the current localhost scan path, which only uses
`../ScanRecord.js`.

They are kept here as design reference for possible future work so the live
`models/` directory reflects the submitted S3/IAM auditor instead of implying
unfinished authentication or workspace features.
